/**
 * RESERVED SLOT — the get-set-up onboarding checklist mounts here.
 *
 * TODO(onboarding stage): build the checklist (step tracking, dismiss state,
 * re-triggerable from Settings) and render it from this component. Until then
 * this renders nothing on purpose; it exists so the dashboard composition and
 * the future checklist land without moving anything around.
 */
export function OnboardingSlot() {
  return null;
}
