// Shared site constants for the store dashboard. Kept in one place so a shared
// package can be extracted later.

export const SITE_NAME = "Square Share";

/** This app: the creator/seller dashboard. */
export const DASHBOARD_URL = "https://store.squareshare.to";

/** The marketplace / marketing site (waitlist + public pages). */
export const MARKETPLACE_URL = "https://squareshare.to";

/** Parent domain the session cookie is scoped to in production. */
export const COOKIE_DOMAIN = ".squareshare.eu";
